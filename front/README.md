
  # AgroPrecision

  This is a code bundle for AgroPrecision. The original project is available at https://www.figma.com/design/ieVtw3rEMDPYLbsBghZgy0/AgroPrecision.

  ## Running the code

  Install node.js (https://nodejs.org/en/download)

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
